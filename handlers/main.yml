---
- name: "Ensure Puppet/OpenVox agent is started and enabled"
  ansible.builtin.service:
    name: "{{ puppet_agent_service }}"
    state: "{{ puppet_agent_service_state }}"
    enabled: "{{ puppet_agent_service_enabled }}"
  when: puppet_agent_service_manage
