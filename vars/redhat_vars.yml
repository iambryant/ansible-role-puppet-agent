---
puppet_release_map:
  puppet: "https://yum.puppetlabs.com/puppet{{ puppet_version }}-release-el-{{ ansible_facts['distribution_major_version'] }}.noarch.rpm"
  openvox: "https://yum.voxpupuli.org/openvox{{ puppet_version }}-release-el-{{ ansible_facts['distribution_major_version'] }}.noarch.rpm"

puppet_release_rpm: "{{ puppet_release_map[puppet_variant] }}"
